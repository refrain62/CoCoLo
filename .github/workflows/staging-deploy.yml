name: staging deploy（Free期間は無効）
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  deployment-disabled:
    # GitHub Free期間はsecret、environment、deployへ到達させない。
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: exit 1
