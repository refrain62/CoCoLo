# AGENTS.md

何も指示がない場合はシニアエンジニアとして対応し、ジュニアエンジニアでもわかる内容で回答してください。

## Autonomous Workflow Policy

### Token Efficiency & Output Policy
- **内部思考（Reasoning）:** トークン消費削減と推論精度向上のため、思考・計画・コード解析は内部的に**英語**で行ってください。
- **ユーザーへの返答:** 報告・質問・確認はすべて**日本語**で行ってください。
- **簡潔性の徹底:** 挨拶や過度な前置きは省き、変更点・テスト結果・次のアクションのみを必要最低限かつ端的に報告してください。

### Coding & Refactoring Principles
- **最小差分の原則:** 依頼された機能・修正に直接関係のないコードのリファクタリングやフォーマット変更を行わないでください。
- **既存スタイルの尊重:** 既存の命名規則、設計パターン、ファイル構成に厳格に従ってください。
- **必要最低限の説明:** 公開APIや責務が複雑なメソッドには、何を保証する処理かが分かる日本語の短い説明を残してください。自明な処理への過剰なコメントは避けてください。
- **設計意図の記録:** コードだけでは読み取りにくい前提、制約、競合対策、トレードオフは、近接する日本語コメントまたは`/docs`配下のドキュメントに残してください。

### Git & GitHub Rules
- **Branch Strategy:** 直接 `main` や `develop` ブランチへコミットや push をしないでください。すべての機能開発は `develop` ブランチを起点とし、専用の作業用ブランチ（例: `feature/xxx`）を作成して作業を行ってください。機能単位でブランチを切り替え、別機能の変更を同じブランチへ混在させないでください。機能の実装・敵対的レビュー・修正・検証・Draft PR作成が完了してから、次の機能ブランチへ切り替えてください。リモートに `develop` が存在しない場合は、機能ブランチを作る前に `origin/main` から `develop` を作成し、ローカル追跡ブランチも同期してください。
- **Commit Granularity:** 意味のある単位でこまめに**日本語**でコミットを作成し、メッセージは変更内容が伝わるよう簡潔に記述してください。
- **Push & PR:** feature単位の実装完了時、リモートに push し、`gh` CLI で `develop` 宛ての Draft PR を作成してください。タイトルと概要はレビュアーが把握できる要約を日本語で記述してください。
- **PR本文:** `.github/pull_request_template.md` と `docs/pull-request-guidelines.md` の7区画・改行・検証ルールに従い、`pnpm verify:pr-description`を通過させてください。

### Verification Rules
- **Package Manager:** パッケージ管理には `pnpm` を使用してください。
- **Auto Verification:** コード変更後は、必ずローカルで `pnpm test`（または指定コマンド）および `pnpm build` を実行して自動検証してください。
- **Self-Correction:** エラーが発生した場合は、原因を特定して解決するまで自動で修正を行ってください。

### Destructive Operations
- 未コミットの変更破棄（`git reset --hard`、`git clean`）や、リモート破棄（`git push --force`）を行う前に、必ず状況と理由を人間に報告して承認を得てください。

## Feature Implementation Rules

- `/docs` 配下の現行プランと仕様を実装の起点とし、機能仕様 ID と実装範囲を確認してから着手してください。
- 機能ごとに専用ブランチを作成・切り替え、別機能の変更を混在させないでください。実装、テスト、敵対的レビュー、指摘修正、再検証、Draft PR作成が完了するまで次の機能へ進まないでください。
- レビューしやすい小さな単位でコミットし、各コミットメッセージと変更説明は日本語で記述してください。
- 機能単位の実装完了時は、テナント越境、認可、個人情報、入力検証、状態遷移、テスト不足、仕様不整合を敵対的に確認し、Critical / High の指摘を解消してから完了扱いにしてください。
- タスク完了時は、実装PRとは別に、そのタスクに関する台帳、仕様、運用記録だけを含むPRを作成してください。実装コードや別タスクの変更を混在させず、レビューと必要なCIが完了した後に `develop` へマージしてください。
- 再開時の最初の読込対象は `docs/resume-task-list.md` だけにしてください。完了済みの実装内容、検証結果、レビュー履歴は `docs/resume-task-history.md` に置き、現行タスクが参照する場合、再検証する場合、状態差分を調査する場合、監査記録が必要な場合だけ該当見出しを読んでください。履歴文書を再開時に全文読み込みしないでください。
- タスクを完了扱いにしたときは、詳細な実施記録を `docs/resume-task-history.md` へ移し、`docs/resume-task-list.md` から完了項目を削除してください。残タスク側には、現在の停止条件と履歴への参照だけを残してください。
