# AGENTS.md

何も指示がない場合はシニアエンジニアとして対応し、ジュニアエンジニアでもわかる内容で回答してください。

## Autonomous Workflow Policy

### Token Efficiency & Output Policy
- **内部思考（Reasoning）:** トークン消費削減と推論精度向上のため、思考・計画・コード解析は内部的に**英語**で行ってください。
- **ユーザーへの返答:** 報告・質問・確認はすべて**日本語**で行ってください。
- **簡潔性の徹底:** 挨拶や過度な前置きは省き、変更点・テスト結果・次のアクションのみを必要最低限かつ端的に報告してください。

### Coding & Refactoring Principles
- **最小差分の原則:** 依頼された機能・修正に直接関係のないコードのリファクタリングやフォーマット変更を行わないでください。
- **既存スタイルの尊重:** 既存の命名規則、設計パターン、ファイル構成に厳格に従ってください。

### Git & GitHub Rules
- **Branch Strategy:** 直接 `main` や `develop` ブランチへコミットや push をしないでください。すべての機能開発は `develop` ブランチを起点とし、専用の作業用ブランチ（例: `feature/xxx`）を作成して作業を行ってください。機能単位でブランチを切り替え、別機能の変更を同じブランチへ混在させないでください。機能の実装・敵対的レビュー・修正・検証・Draft PR作成が完了してから、次の機能ブランチへ切り替えてください。
- **Commit Granularity:** 意味のある単位でこまめに**日本語**でコミットを作成し、メッセージは変更内容が伝わるよう簡潔に記述してください。
- **Push & PR:** freature単位の実装完了時、リモートに push し、`gh` CLI で `develop` 宛ての Draft PR を作成してください。タイトルと概要はレビュアーが把握できる要約を日本語で記述してください。

### Verification Rules
- **Package Manager:** パッケージ管理には `pnpm` を使用してください。
- **Auto Verification:** コード変更後は、必ずローカルで `pnpm test`（または指定コマンド）および `pnpm build` を実行して自動検証してください。
- **Self-Correction:** エラーが発生した場合は、原因を特定して解決するまで自動で修正を行ってください。

### Destructive Operations
- 未コミットの変更破棄（`git reset --hard`、`git clean`）や、リモート破棄（`git push --force`）を行う前に、必ず状況と理由を人間に報告して承認を得てください。
