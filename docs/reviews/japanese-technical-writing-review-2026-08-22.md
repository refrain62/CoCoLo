# 日本語表記の敵対的レビュー

## 対象

- 基準ブランチ: `develop`
- 作業ブランチ: `feature/japanese-technical-writing-review`
- 対象コミット: `ec4c779`、`8f03eec`、`868b8e6`
- 対象範囲: 現行の画面、API、契約、運用スクリプト、GitHub Actions、仕様書、環境設定例
- 対象外: `docs/reviews/` 配下の過去レビュー記録。過去の証跡は内容を改変しない。

## 実施内容

- 画面・API・Zod 契約のエラー文を、利用者が原因と対応を判断できる日本語へ統一した。
- 年度繰り上げの画面説明を、実際の認可条件である「オーナーまたは管理者」と一致させた。
- 配置アダプター、成果物、テストデータ、リクエストなど、運用ログと CI の説明語を統一した。
- 仕様書へ用語・表記ルールを追加し、`.env.example` に各環境変数の用途と設定内容を記載した。
- OpenAPI の生成元を変更した後、`packages/contracts/openapi.yaml` を再生成した。

## 敵対的確認

| 観点 | 結果 |
| --- | --- |
| 認証・認可 | 文言だけを変更し、認証・認可ロジックは変更していない。年度繰り上げの画面表示だけ、実装済みの owner / admin 条件に修正した。 |
| テナント境界 | テナント ID、所属解決、RLS、リポジトリの検索条件は変更していない。 |
| 個人情報 | 画面表示項目、ログ出力項目、監査対象は変更していない。 |
| 入力検証 | Zod の検証条件、エラー項目パス、HTTP ステータスは変更していない。エラー文と対応するテスト期待値だけを更新した。 |
| 状態遷移・競合 | 年度繰り上げの状態値、冪等性キー、リクエストハッシュの判定は変更していない。内部エラー文だけを日本語化した。 |
| 生成物・CI | OpenAPI の生成物を同期し、ワークフローの実行内容と Action の SHA 固定は維持した。 |

## 判定

- Critical: 0件
- High: 0件
- Medium: 0件
- 判定: クリア

## 検証結果

- `pnpm test`: 成功
- `pnpm test:unit`: 成功（Vitest 4ファイル・8テスト、API 22テストを含む）
- `pnpm build`: 成功
- `pnpm lint`: 成功
- `pnpm typecheck`: 成功
- `pnpm lint:biome`: 成功
- `pnpm lint:openapi`: 成功
- `pnpm lint:workflows`: 成功
- 統合テスト: 実行を試みたが、ホストから PostgreSQL（`localhost:5432`、公開ポート `55432`）へ接続できず未完了。Docker コンテナ内部の PostgreSQL は稼働中であることを確認した。文言変更に起因するエラーではない。
